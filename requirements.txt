# 核心后端框架
flask
flask-cors
flask-sqlalchemy
flask-migrate

# 认证与安全
PyJWT
bcrypt

# 云存储
cloudinary

# 生产服务器
gunicorn

# 数据库驱动（根据你使用的数据库选择）
psycopg2-binary  # PostgreSQL
# pymysql         # MySQL (如果使用 MySQL，取消注释此行)

# 环境变量加载（仅开发环境需要，生产环境通常由 Railway 直接设置）
python-dotenv
